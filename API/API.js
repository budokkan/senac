// Por que o DOM é importante?

// Permite modificar o conteúdo da página de forma dinâmica
// (adicionar, remover, alterar elementos).
// Possibilita interação com o usuário através de eventos
// (cliques, digitação, etc.).
// Permite aplicar estilos e manipular o layout em tempo real.
// Imagine mudar a cor de fundo da página com um clique,

// Como acessar elementos DOM com JavaScript?

// document.getElementById("id"): Retorna um elemento pelo seu ID.
// document.getElementsByClassName("class"): Retorna todos os
// elementos que possuem uma determinada classe.
// document.getElementsByTagName("tag"): Retorna todos os elementos
// de um determinado tipo de tag (como div, p).
// document.querySelector("seletor"): Retorna o primeiro elemento que
// corresponde a um seletor CSS.

// Interagindo com o DOM através de eventos

// Eventos são ações ou ocorrências que podem ser detectadas
// pelo JavaScript (ex.: clique, tecla pressionada, movimento
// do mouse).

// onclick: Acionado quando o usuário clica em um elemento.
// onmouseover: Acionado quando o mouse passa sobre um elemento.
// onsubmit: Acionado quando um formulário é enviado.